# Runtime database used by the Next.js app.
DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require

# Safety latch for `npm run db:seed`; enable only against an isolated dev/test DB.
# ALLOW_DATABASE_SEED=true
